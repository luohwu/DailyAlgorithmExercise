# https://leetcode.com/problems/sort-colors/submissions/

from typing import List

class Solution:
    def sortColors(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """

        # need sort solution instead of this method
        # n = len(nums)
        # table = [0] * 3
        # for num in nums:
        #     table[num] += 1
        # nums[:table[0]] = [0] * table[0]
        # nums[table[0]:table[0] + table[1]] = [1] * table[1]
        # nums[table[0] + table[1]:] = [2] * table[2]

